{
    "name": "tcp-builder",
    "version": "1.0.0",
    "main": "tcp.js",
    "description": "Custom TCP packet builder utilities for OS detection and network protocol probes",
    "keywords": ["tcp", "networking", "packet", "tcp-packet", "nodejs", "network-tools"]
}
